# P6-M AsPredicted Question 8 draft

Paste only after confirming that the preview displays the hashes exactly and
P5 #307836 is either bundled as overlapping or disclosed below.

> This prospective P6-M study is informed by released P1.1–P5 evidence,
> including P5 #307836, but no P6-M seed-4 parent, child, validation scalar,
> restricted-test scalar, topology comparison, or P6-specific model-selection
> outcome has been collected or viewed before filing. P6-M is therefore
> prospective but not outcome-independent. After filing, authority is the
> filed PDF; the prefiling addendum at SHA-256
> `df49664809ada69d23dcd2c799e75b30f0fdb9afd7aee12b071ac24ef2f81082`;
> the unmodified gate plan at SHA-256
> `d8a63608608c59d2c4d9882e5346625462056c331094942a8a01d496697a1c79`;
> then the P6 LOCK, manifests, and deterministic receipts. The addendum binds
> topology manifest SHA-256
> `d1e7d5af7247a572e319ee003b5f4e3da5d1fb1592e5ed9ff6b22eeec15ea606`.
> All mixed arms use exact source-content quotas of 9,633,792 Tagalog and
> 9,633,792 English tokens, identical within-language streams, one stored
> no-wrap schedule, and no runtime reshuffle. M-fine uses 2,048-token EN-first
> alternating blocks; M-coarse uses 1,204,224-token EN-first alternating
> blocks; M-blocked is all Tagalog then all English; M-rand is one precomputed
> sequence of 4,704 blocks per language, shuffled once with Python
> `random.Random(42)` and hash-pinned before filing. All six child conditions ×
> two languages are sealed before exactly one authorized M-fine-only secondary
> restricted-test event and one formal unblinding. C1, C2, M-coarse,
> M-blocked, and M-rand are not tested, and no cross-topology test comparison
> is computed. Technical recovery follows the addendum's pre-outcome taxonomy
> from immutable inputs; valid terminal checkpoints are accepted without
> retraining, while outcome-bearing or unrecoverable protocol stops are
> reported rather than replaced. P6-M measures BPB schedule topology, not
> downstream Filipino-task performance or all Philippine languages.
