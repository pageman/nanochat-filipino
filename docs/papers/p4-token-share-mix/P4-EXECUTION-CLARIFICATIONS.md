# P4 execution clarifications

**Purpose:** Define operational terms that **do not amend** a future filed P4 PDF.  
**Acceptance:** After filing, dated only if they do **not** alter F4 scientific quantities. SHA recorded in LOCK. Cannot override PDF. A “pre-Gate-A addendum” that changes \(q_{\mathrm{TL}}\), \(\delta\), tokenizer hashes, seeds, or test policy is an **amendment**, not a clarification.

Authority: filed PDF ≫ these clarifications ≫ LOCK ≫ run cards ≫ deviations ≫ chat.

## Terms

| Term | Meaning |
|---|---|
| Confirmatory | Official `val_bpb_full` after terminal ckpt; filed contrasts |
| Descriptive | Byte shares, fertility, C0 EN val, test BPB, in-loop val |
| Exploratory | Anything not in the PDF; labeled as such; no arm selection |
| Safe progress | Status/hashes/counts/finite; never BPB or signs |
| Lockbox | Mode-600 / encrypted outcomes until Gate X |
| Technical stop | Hardware/path/integrity fault; not a scientific finding |
| Protocol stop | Confirmatory label invalidated unless a pre-filed path applies |
| Clean restart | From immutable C0, fresh optimizer, full N steps; quarantine partial |

## Carry-forward vs outcome-tuning

Mechanical carry-forward of T, B, N, 0.01-family cutoff, and (if signed) tokenizer hash is **allowed** as post-P3 apparatus continuity. Using P3 \(C_{\mathrm{tl}}\), \(G_{\mathrm{en}}\), B3 cells, or Gate V numbers to pick \(q_{\mathrm{TL}}\) or δ **is forbidden**.

## “Mitigation”

Forbidden in title, abstract, and AsPredicted until Gate X. After X, only if both co-primary criteria met, with the protocol’s narrow sentence.

## Scripts

`scripts/p4/` **do not exist yet**. Creating them is **post-filing Gate A work**, not a scientific choice.

## P0-T device

Authoritative P0-T eligibility is **CUDA-only** on the filed class. A CPU evaluation, if run, is diagnostic and **MUST NOT** set PASS/BLOCKED.

## C3 val.parquet

Trainer-interface last-shard hygiene only. **MUST NOT** provide a gate signal or replace Gate U JSONL evaluations.

## Hosts

Do not name a live pod in the PDF. File a **class** (e.g. NVIDIA A40 48 GB). HOST SSH cards stay gitignored.
