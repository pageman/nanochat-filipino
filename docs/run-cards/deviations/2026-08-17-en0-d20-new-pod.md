# Execution-only — EN0 d20 on a new A40 because terminate-after is create-only

- Date (UTC): 2026-08-17
- Operator: Paul Pajo
- Classification: **host swap, not a science change**

## Why

Gate H / EN0 d8 ran on `xk8orhscuk2jsu` with auto-terminate `2026-08-17T19:05:34Z`. At d20 launch that left ~80 minutes. Observed d20 step time is ~5.33 s → ~8.3 h wall. `runpodctl pod update` and MCP `update-pod` have no `terminateAfter` field; GraphQL `podEditJob` also omits it.

## What changed

Created `8ik4ix7j8iju9u` (`p2-en0-d20`), same image and NVIDIA A40 48 GB Secure EU-SE-1, volume 80 GB, `--terminate-after 2026-08-18T03:49:08Z`. Copied the English pack from this Mac. Did **not** resume d8 weights. Did **not** change \(T\), \(B\), \(N_{\mathrm{EN0}}\), warmup, eval tokens, or depths.

## What did not change

AsPredicted #306935, pin `92d63d4`, English BPE, `en-active` only, final-checkpoint selection, CORE off, no P1.1 parent, no Tagalog.

The $3.91 figure remains a planning estimate, not a commitment. Two pods overlap until `xk8orhscuk2jsu` auto-terminates at 19:05Z.
