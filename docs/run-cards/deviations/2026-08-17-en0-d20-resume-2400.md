# Execution-only — EN0 d20 resume from step 2400 after volume-full stop

- Date (UTC): 2026-08-17T22:45Z
- Operator: Paul Pajo
- Classification: **official nanochat resume + disk hygiene, not a science change**

## What happened

`save-every=200` filled the 80 GB volume at step 2600. `optim_002600_rank0.pt` was truncated (128 MiB vs 3.50 GiB). Trainer exited with no traceback. Last complete bundle: step **2400**.

## What we did

- Quarantined truncated 2600 files under `/workspace/incomplete-ckpt-2600/`
- Deleted intermediate 200–2200 (diagnostic only)
- Kept complete `model_002400.pt` + `optim_002400_rank0.pt` + `meta_002400.json`
- Resumed with pin `base_train --resume-from-step=2400 --num-iterations=5415 --save-every=-1`
- Same `T=2048`, `B=65536`, depth 20, warmup 40, eval-tokens 124438, unpatched SDPA, `window_pattern=SSSL`

`save-every=-1` is the protocol-allowed final-only cadence so the remaining ~4.5 h cannot refill 80 GB.

## What we did not do

No attention patch, no `window_pattern=L`, no FP8, no new optimizer, no arXiv/SOTA trainer swap, no P1.1 weights, no Tagalog.

Wall-clock gap: last step 2599 at 21:57Z; resume 2400 at 22:45Z (~48 min idle). Selection remains **final `model_005415.pt`**.
