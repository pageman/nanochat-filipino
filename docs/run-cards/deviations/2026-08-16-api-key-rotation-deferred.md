# Execution-only — API key rotation deferred until after Gate I

- Date (UTC): 2026-08-16
- Operator: Paul Pajo
- Source: operator instruction in chat, overriding the pre-launch checklist no-go

## Decision

The checklist required rotating the exposed Runpod API key before Gate I. The operator explicitly deferred rotation until after this work. Gate I proceeds on the existing key. Rotation remains owed; it is not waived.

This does not change depths, `T`, `B`, `N`, ratios, tokenizer, corpus, or the `D*` rule.
