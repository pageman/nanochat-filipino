# Dear Reader — P5 (multi-seed panel)

**Paste this into the ResearchBox “Dear Reader” field for box [#8904](https://researchbox.org/8904).** Not for P1.1 (#8735), P2 (#8763), P3 (#8834), or P4 (#8869). Do **not** paste the box passcode here or into git.

---

Dear Reader,

Thank you for opening **ResearchBox #8904**, the deposit box for **NANOCHAT-FILIPINO P5** (“How often a frozen mix recurs across unused initializations: a closed three-seed count”).

Results provenance on AsCollected: **#2503** Version 1 — https://ascollected.org/HC8_G2F

This note exists because (1) P5 is easy to confuse with P4 and prior studies, (2) ResearchBox’s bingo columns are awkward for a computational LM panel study, (3) several absences are intentional, and (4) **P5 does not confirm P4 as law** — it reports a predeclared count over three fresh parent seeds.

---

## What this box is

- **AsPredicted #307836** — https://aspredicted.org/k6ib64.pdf  
- A **closed panel** of seeds `{1,2,3}` on the **frozen P4 token-share apparatus** (q_TL=0.50; C3 is **not** P3 B3).
- Run ID **`p5-20260823T160632Z-439d1de5`**, Gates **0 / A–H / I₁–V₃ / X / W** complete.
- **Primary result:** k_both=**3** of K_elig=**3** (count table; no mean, CI, or p-value).

## What this box is not

- **Not** ResearchBox #8869 (P4 single-seed study).
- **Not** a human-subjects study.
- **Not** raw held-out `test.jsonl` text.
- **Not** model `.pt` weights (Hub deferred: C0+C1+C2+C3 per eligible seed together).
- **Not** an amendment to P1.1, P2, P3, or P4 registrations.

## Bingo column map

| Column | Contents |
|---|---|
| **Preregistration** | AsPredicted #307836 only |
| **Materials** | Protocol, LOCK (sanitized), paper |
| **Data** | CSV tables (panel count + per-seed crosstab + hashes) |
| **Code** | `scripts_p5/*.py` and `*.sh` only |
| **Other** | JSON gate receipts and released seals |

Hex strings prefixed `sha2-256:` are **file digests**, not participant IDs.

## Spot-check

- Panel: both=3, only-R=0, only-A=0, neither=0, ineligible_parent=0  
- Per-seed classes: all `both`  
- C3 is not P3 B3; P4 seed 0 is not a P5 confirmatory cell

— End of Dear Reader (P5) —
